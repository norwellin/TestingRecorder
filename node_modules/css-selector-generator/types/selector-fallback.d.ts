import { CssSelector } from "./types.js";
/**
 * Creates fallback selector for single element.
 */
export declare function getElementFallbackSelector(element: Element, root?: ParentNode): CssSelector;
/**
 * Creates chain of :nth-child selectors from root to the elements.
 */
export declare function getFallbackSelector(elements: Element[], root?: ParentNode): CssSelector;
